# Clerk Authentication (required for multi-user mode)
# Get your keys at https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Auth mode
# Set to "true" to disable authentication (single-user mode for self-hosting)
# Note: Set BOTH variables to ensure client and server are in sync
DISABLE_AUTH=false
NEXT_PUBLIC_DISABLE_AUTH=false

# Clerk URLs (optional - defaults work for most setups)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Database path (optional - defaults to ./data/pulse.db)
# DATABASE_PATH=/path/to/pulse.db

# Sprint Orchestrator API Key (required for agent API access)
# Generate a secure random key for authenticating orchestrator requests
# Example: openssl rand -hex 32
ORCHESTRATOR_API_KEY=your-secure-api-key-here
